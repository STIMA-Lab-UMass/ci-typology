[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
markers =
    network: marks tests that need network/OpenAI access (auto-skipped offline)
filterwarnings =
    ignore::DeprecationWarning
